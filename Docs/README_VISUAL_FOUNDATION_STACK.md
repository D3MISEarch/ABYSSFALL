# Visual Foundation v0.1 Stack Note

This branch is intentionally stacked on PR #130's revised camera head for one combined owner playtest. Review and package the visual-foundation branch as a dependency of `codex/dopamine-camera-director`; do not treat it as independently mergeable to `main` until the camera dependency is resolved.
